// JavaScript Document


function valida(form){
	/*
	if (form.nome.value=="" || form.nome.value=="Nome") {
		alert("Preencha o nome corretamente.");
		form.nome.focus();
		return false;
	}
	
	var filtro_mail = /^.+@.+\..{2,3}$/
	if (!filtro_mail.test(form.email.value) || form.email.value=="" || form.email.value=="E-mail") {
		alert("Preencha o e-mail corretamente.");
		form.email.focus();
		return false;
	}
	*/
	alert(form.nome.value . form.email.value . form.telefone.value . form.telefone.value . form.assunto.value . form.area.value . form.mensagem.value);
	
	/*
	if (form.telefone.value=="" || form.telefone.value=="Telefone") {
		alert("Preencha o telefone corretamente.");
		form.telefone.focus();
		return false;
	}
	
	if (form.assunto.value=="" || form.telefone.value=="Assunto") {
		alert("Preencha o nome corretamente.");
		form.assunto.focus();
		return false;
	}
	
	if (form.area.value=="Selecione uma àrea...") {
		alert("Selecione uma àrea de atendimento.");
		form.area.focus();
		return false;
	}
	
	if (form.mensagem.value=="" || form.mensagem.value=="Sua Mensagem") {
		alert("Preencha sua mensagem.");
		form.mensagem.focus();
		return false;
	}
*/
}
 

